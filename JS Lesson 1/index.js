// let currentResult = 0; //Initiliazed to zero

// currentResult = currentResult + 10;



function add (num1, num2) {
    const result = num1 + num2;
    // alert( "The Result is " + result);
    return result;

}

add(5, 3);
add(10, 5);

addEventListener(type, listener)

